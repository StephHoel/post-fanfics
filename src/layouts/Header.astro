---
import { getSortedFicEntries } from "../utils/getSortedFicEntries"
---

<header class="bg-gray-800 shadow-md w-full">
  <div class="w-3/4 mx-auto flex justify-between items-center p-4">
    <a
      href="/post-fanfics/"
      class="text-2xl font-bold text-white hover:text-gray-400 cursor-pointer"
    >
      📚 Fanfics
    </a>

    <nav class="relative mr-2">
      <div class="group inline-block relative" tabindex={0}>
        <button
          class="text-lg font-bold text-white rounded px-4 py-2 focus:outline-none cursor-pointer"
        >
          Minhas Histórias
        </button>

        <div
          class="absolute right-0 pt-2 w-56 bg-gray-900 rounded shadow-lg opacity-0 group-hover:opacity-100 group-focus-within:opacity-100 transition-opacity duration-200 z-10 pointer-events-none group-hover:pointer-events-auto group-focus-within:pointer-events-auto"
        >
          <ul>
            {
              getSortedFicEntries().map(([slug, name]) => {
                return (
                  <li>
                    <a
                      href={`/post-fanfics/fanfics/${slug}`}
                      class="block px-4 py-2 text-gray-300 hover:text-white hover:bg-gray-700 rounded"
                    >
                      {name}
                    </a>
                  </li>
                )
              })
            }
          </ul>
        </div>
      </div>
    </nav>
  </div>
</header>
